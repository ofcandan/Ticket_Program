<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="beispiel.Views.Login">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Benutzername" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" />

            <Entry VerticalOptions="Center" Placeholder="Benutzername eingeben" Text="{Binding Username}" HorizontalTextAlignment="Center"/>

            <Label Text="Passwort" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" />

            <Entry VerticalOptions="Center" Placeholder="Passwort eingeben" Text="{Binding Password}" HorizontalTextAlignment="Center"/>

            <Button WidthRequest="100"  HeightRequest="50" Text="Anmelden" Command="{Binding LoginCommand}" Margin="0,0,0,100"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>